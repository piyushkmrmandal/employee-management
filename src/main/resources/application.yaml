spring:
  application:
    name: employee-management

  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    url: jdbc:h2:file:/Users/piyush/Documents/learning/codingshuttle/employee-management/src/main/resources/db
    username: piyush
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
